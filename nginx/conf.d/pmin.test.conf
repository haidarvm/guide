#test pmin
server {
    server_name    pmin.test;
    root           /var/www/pmin;
    error_log      /var/log/nginx/pmin-error.log error;

    index          index.php index.html;
    location / {
                try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ /\.  { return 403; }

    include /etc/nginx/default.d/*.conf;
}
